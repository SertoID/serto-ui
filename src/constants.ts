export enum routes {
  HOMEPAGE = "/",
  ADMIN = "/admin",
  LOGIN = "/login",
  TENANT = "/tenant",
  FEEDS = "/tenant/feeds",
  ISSUED_CREDENTIAL = "/tenant/issued-credentials",
  RECEIVED_CREDENTIAL = "/tenant/received-credentials",
}
